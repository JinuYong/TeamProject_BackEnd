<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.omymbackend.dao.UserDAO">
    <insert id = "postUser" parameterType="com.example.omymbackend.model.User">
        INSERT INTO users (
            IDX,
            ID,
            PASSWORD,
            EMAIL,
            NAME,
            PHONE,
            ROLES,
            REGISTER_TIME,
            DELETE_YN,
            POSTCODE,
            ADDRESS,
            DETAIL_ADDRESS
        ) VALUES (
            SQ_USERS.NEXTVAL,
            #{id},
            #{password},
            #{email},
            #{name},
            #{phone},
            'ROLE_USER',
            sysdate,
            'N',
            #{postcode},
            #{address},
            #{detailAddress}
            )
    </insert>

    <select id = "getById" parameterType="String" resultType="int">
        SELECT COUNT(*)
        FROM USERS
        WHERE ID = #{id}
    </select>
</mapper >